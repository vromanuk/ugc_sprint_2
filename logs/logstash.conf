input {
    beats {
        port => 5045
        host => "0.0.0.0"
        tags => ["nginx"]
        codec => "json"
      }
}
filter {
}
output {
  elasticsearch {
    hosts => [ "${ES_HOST}" ]
    index => "nginx-%{+YYYY.MM.dd}"
  }
}
